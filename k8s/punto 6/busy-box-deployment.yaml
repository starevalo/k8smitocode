apiVersion: v1
kind: Pod
metadata:
  name: busybox-curl-pod
  namespace: nspace1
spec:
  containers:
    - name: busybox-curl-container
      image: yauritux/busybox-curl:latest
      command: ["sleep", "infinity"]